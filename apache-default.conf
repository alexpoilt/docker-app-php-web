<VirtualHost *:80 >
    ServerName ${DOMAIN_NAME}
    DocumentRoot ${DIR_WWW}
        <Directory "${DIR_WWW}">
            Options FollowSymLinks
            AllowOverride All
            Order allow,deny
            Allow from all
        </Directory>
</VirtualHost>

